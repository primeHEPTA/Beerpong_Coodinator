First create a folder called "_tournaments" in the same directory then run game_phase.py to launch the tournament.
You can load different tournaments, which will be automatically stored in the "_tournaments" directory.
